NODE_ENV=development
HOST=127.0.0.1
PORT=4173

# MySQL / MariaDB. En Hostinger el nombre suele incluir el prefijo de la cuenta.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=RADC28
DB_USER=radc28_user
DB_PASSWORD=change-me
DB_CONNECTION_LIMIT=10
DB_SSL=false

# Exactamente 64 caracteres hexadecimales. Genere uno con:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
FIELD_ENCRYPTION_KEY=replace-with-64-hex-characters

# Solo se usa si la tabla de usuarios esta vacia.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-temporary-password
ADMIN_NAME=Administrador RAD-C28

SESSION_HOURS=8
TRUST_PROXY=1
